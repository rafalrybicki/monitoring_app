<%= turbo_stream.append "tasks", @task %>

<% if @task.date = today %>
  <%= turbo_stream.update "#{today}-percentage", day_percentage %>
<% end %>

<%#= turbo_stream.update Task.new, partial: 'tasks/form', locals: { task: Task.new } %>

<%= render_random_quote %>
